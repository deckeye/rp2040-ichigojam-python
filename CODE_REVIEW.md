# コードレビュー修正サマリー

## 修正された問題点

### 1. ✅ LED関数の状態管理
**問題**: LED(-1)でトグルする際、毎回新しいPinオブジェクトを作成していたため、前の状態を取得できませんでした。

**修正**: グローバル変数 `_led_pin` を導入し、Pinオブジェクトを再利用するように変更。

### 2. ✅ PWMリソースリーク
**問題**: PWM関数がState Machineを解放せず、リソースリークが発生していました。

**修正**: 
- `_active_pwm` 辞書でアクティブなPWMを管理
- 同じピンに新しいPWMを設定する際、古いものを自動停止
- `PWM_STOP(pin)` 関数を追加してユーザーが明示的に停止可能に

### 3. ✅ IOT_GETのURL解析バグ
**問題**: `https://example.com/path` 形式のURLが正しく解析されませんでした。

**修正**: `://` を基準にプロトコルとホスト/パスを分離する改善されたパーサーを実装。

### 4. ✅ WIFI接続の無限ループ
**問題**: 接続できない場合に永久に待機していました。

**修正**: 30秒のタイムアウトを追加し、接続失敗時にエラー表示。

### 5. ✅ importの配置
**問題**: モジュールのimportがファイルの途中に散在していました。

**修正**: すべてのimportをファイル先頭に移動（Pythonの慣例に準拠）。

### 6. ✅ HELPデータの不足
**問題**: 新規追加関数（IOT_GET, WIFI, CORE2等）のヘルプ情報が欠落していました。

**修正**: すべての関数のヘルプ情報を `_HELP_DATA` に追加。

### 7. ✅ 型チェックの改善
**問題**: `isinstance(d, tuple) or isinstance(d, list)` が冗長でした。

**修正**: `isinstance(d, (tuple, list))` に簡潔化。

## 追加改善

- **定数化**: ピン番号（LED=25, BUZZER=15, BUTTON=14）を定数として定義
- **docstring追加**: すべての関数にdocstringを追加
- **HELPのソート**: `HELP()` の出力をアルファベット順にソート
- **エンコーディング修正**: `_iot_request` で `.encode()` を明示的に使用

## 影響範囲

- **破壊的変更**: なし（すべて後方互換性を維持）
- **新規追加**: `PWM_STOP(pin)` 関数
- **動作変更**: LED(-1)が正しく動作、PWMが適切にリソース管理、WIFI接続が30秒でタイムアウト

## テスト推奨項目

1. `LED(-1)` を複数回実行してトグル動作を確認
2. 同じピンで `PWM()` を複数回呼び出してリソースリークがないことを確認
3. `IOT_GET("https://example.com/path/to/resource")` でURL解析を確認
4. 存在しないWiFiに接続して30秒でタイムアウトすることを確認
